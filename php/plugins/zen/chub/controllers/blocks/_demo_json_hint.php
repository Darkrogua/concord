<p class="help-block" style="margin-bottom: 16px;">
    JSON на диске в <code>partials/{group}/</code>.
    Базовый <code>{name}.json</code> — fallback для всех вариантов.
    Файл <code>{name}.{variant}.json</code> — только если создан на диске (override demo для варианта).
    Ключи верхнего уровня — id demo-сценария (<code>default</code>, <code>warning</code> …).
</p>
